import React from "react";

export const Component = () => {
    return (
        <article>
            <h2>CSS (Cascading Style Sheets)</h2>
            <ul>
                <li><span>Definition: </span>CSS is a style sheet language used for describing the presentation of a document written in HTML or XML.</li>
                <li><span>Description: </span>CSS allows developers to control the layout, appearance, and styling of web pages. 
                    It facilitates the separation of content and presentation, making it easier to maintain and update the visual aspects of a website. 
                    CSS uses selectors to target HTML elements and apply styles like colors, fonts, and positioning.
                </li>
            </ul>
        </article>
    );
}