import React from "react";

export const Component = () => {
    return (
        <article>
            <h2>HTML (HyperText Markup Language)</h2>
            <ul>
                <li><span>Definition: </span>HTML is a markup language used to structure and present content on the World Wide Web.</li>
                <li><span>Description: </span>It provides a standardized way to create the basic building blocks of a web page, 
                    such as headings, paragraphs, links, and images. HTML uses tags to define elements, 
                    and browsers interpret these tags to render the content as a visually appealing and structured web page.
                </li>
            </ul>
        </article>
    );
}